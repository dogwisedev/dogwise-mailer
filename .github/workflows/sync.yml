name: Sync to Personal Repo

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Push to Personal Repo
        run: |
          git remote add target https://AlmaSomerville:${{ secrets.MAILER_BACKUP }}@github.com/AlmaSomerville/mailerapp.git
          git push target HEAD:main --force
